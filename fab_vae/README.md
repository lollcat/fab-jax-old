# Flow Annealed Importance Sampling Bootstrap Varational Autoencoder (FAB-VAE)
Requires some changes to the vanilla fab implementation, as now the target distribution $p(z | x)$ is different for 
each x point. 